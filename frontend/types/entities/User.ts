export interface User {
    id: string
    email: string
    name: string
    description: string
    avatarUrl: string
    memberSince: string
    followersCount: number,
    followingCount: number
}